meta {
  name: Delete user target
  type: http
  seq: 40
}

delete {
  url: {{baseUrl}}/users/:userId/targets/:targetId
  body: none
  auth: none
}

params:path {
  userId: 
  targetId: 
}

headers {
  X-Appwrite-Project: {{projectId}}
}
